package com.university.validation;

import java.util.List;
import java.util.Map;

import com.university.model.impl.Student;
import com.university.querybuilder.QueryBuilder;

public class StudentValidator {

	public StudentValidator() {}
	
	public boolean validate(Map<String, String> requestParams) {
		
		List<Student> currentRecords = new QueryBuilder(Student.class).select("*").getAll();
		
		for (Map.Entry<String, String> entry : requestParams.entrySet()) {
			String field = entry.getKey();
			String value = entry.getValue();
			
			return false;
		}
		
		return true;
	}
	
	private boolean validateStudentId() {
		
		return true;
	}
	
	private boolean validateName() {
		
		return true;
	}
	
	private boolean validateMajor() {
		
		return true;
	}
	
	private boolean validatePhoneNumber() {
		
		return true;
	}
}
