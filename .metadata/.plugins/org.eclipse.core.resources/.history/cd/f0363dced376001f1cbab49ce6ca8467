package com.university.validation;

import java.util.Map;

import com.university.model.impl.Student;
import com.university.querybuilder.QueryBuilder;

public class UpdateValidator {
	
	private List<String> phoneNumbers = new ArrayList<>();
	
	public UpdateValidator() {}
	
	public boolean validate(Map<String, Object> updateParams) {
		getStudentPhoneNumbers(updateParams);
		
		String updatedMajor = updateParams.get("major");
		String phoneNumber = updateParams.get("phoneNumber");
		
	}
	
	private void getStudentRecords(Map<String, String> params){
		this.phoneNumbers = new QueryBuilder(Student.class).select("phoneNumber").getAll();
	}
	
	public Map<String, String> getErrorBag(){
		return errorBag;
	}
}
